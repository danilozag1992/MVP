<%= @practice.name %>
<%= button_to 'edit', edit_practice_path(@practice), method: :get, class: 'btn btn-primary' %>
<%= button_to 'delete', @practice, method: :delete, class: 'btn btn-primary', data: { confirm: 'For real?' } %>
<%= button_to 'back', request.referrer, method: :get, class: 'btn btn-secondary' %>